# Spotify Lyrics Widget — Roadmap (V1.3 onward)

Date: 2026-05-25

**Trunk status:** `master` is still at V1 (`d41add2`). V1.1 + V1.2 are committed on branch
`codex/spotify-widget-v1-1-ui-polish` (V1.2 = `56ed38d`), **not yet merged to master**.

This is the high-level roadmap. Each phase still needs its own detailed plan/spec
(brainstorm → spec → plan) in a fresh session before implementation.

---

## Current state (done)

- **V1** — lyrics core: PKCE auth, 1s Spotify poll, LRCLIB lookup + ranking, LRC parse,
  frameless always-on-top widget, config in `%APPDATA%`.
- **V1.1** — UI polish: rounded `#121212` panel, green border, typography, stable hover,
  long-title elide.
- **V1.2** — stability (commit `56ed38d`, tested):
  - Logging to `%APPDATA%/spotify-lyrics-widget/widget.log` + `QMessageBox.critical` on
    fatal/startup/auth errors; `run.pyw` (pythonw, no console).
  - Fixed geometry (no title/lyric jump, 2-line lane, 2nd line never clipped), correct
    first frame.
  - Offline indicator as overlay (does not push content).
  - Single-instance guard (`QLocalServer`/`QLocalSocket`; 2nd launch raises the 1st).
  - 429 backoff: reads `Retry-After`, stops polling for that window, UI shows
    "rate limited - retrying", resumes. Fixes the hammering that caused the real ~15h ban.

---

## Roadmap (consensus: Claude × Codex, 2026-05-25)

| Phase | Contents |
|-------|----------|
| **V1.3** | ① Start-menu / desktop shortcut (**no** startup-on-boot). ② Chinese font → 思源黑體 / Noto Sans TC (title + lyric). |
| **V1.4** | Lyrics quality: LRCLIB stays primary + add **one** optional fallback source (used only when LRCLIB misses; config toggle; explicit failure handling). |
| **V2** | Playback controls (play/pause/next/prev) + hover title marquee (title becomes left-aligned at rest here). |
| **Deferred (split out of V2)** | Add current track to a playlist + remember default playlist. |
| **Later / V3** | PyInstaller single-exe packaging, first-run UX, antivirus false-positive / signing. |

---

## Decisions & rationale (do not revisit without a new reason)

- **No startup-on-boot** (user, 2026-05-25). If ever added, it MUST be gated behind a
  verified V1.2 single-instance guard + 429 backoff — otherwise autostart resurrects the
  zombie / multi-instance hammering that caused the ~15h 429 ban.
- **Log viewer: not built.** V1.2 already pops `QMessageBox` for fatal/startup/auth errors
  and logs everything to `widget.log`. Optional minimal add only if wanted later: an
  "Open log file" action (`os.startfile`). No full in-app viewer (over-engineering for a
  personal tool).
- **Chinese font → 思源黑體 / Noto Sans TC.** Current `Segoe UI` has no CJK glyphs, so
  Windows substitutes an ugly fallback. Noto / Source Han is SIL OFL (free to bundle).
  Ship the font file, load via `QFontDatabase.addApplicationFont`, apply to title + lyric.
  Re-tune the fixed lyric lane height after the swap.
- **V2 trims:** edge-snap CUT (nice-to-have only). Drag-to-resize-width DEFERRED to a later
  version.
- **Auto-resize REJECTED** (content-driven / adaptive). It reintroduces the title/lyric
  jumping that V1.2 Task 2 fixed. The only allowed resize is user-draggable WIDTH persisted
  to config. Do not revisit.
- **Title left-align** folds into V2's marquee rework (not a standalone version). See the
  design spec's "V2 — Playback Controls & Playlist" section.
- **Playlist-add** (add current track to a playlist + remember default) is split out of V2
  into its own later phase; do controls + marquee first.
- **Lyrics fallback (V1.4):** LRCLIB stays primary; add ONE optional fallback at a time,
  behind config + clear failure handling. Candidate (with caveats):
  `akashrchandran/syrics` + `spotify-lyrics-api` (official lyrics via `sp_dc` cookie —
  unofficial, expires, ToS gray area; fallback only, never core).

---

## Pointers

- Tools shortlist: `docs/reference-tools.md` (on master)
- V1.2 plan (tasks + verify steps): `docs/superpowers/plans/2026-05-24-spotify-lyrics-widget-v1-2-stability.md` (on the branch)
- V2 title / marquee + alignment decision: `docs/superpowers/specs/2026-05-22-spotify-lyrics-widget-design.md` → "V2 — Playback Controls & Playlist"

---

## Note on branching

`master` is behind; all V1.1/V1.2 code + the V1.2 plan live on
`codex/spotify-widget-v1-1-ui-polish`. `reference-tools.md`, this roadmap, and the design
V2 note are on master. Recommend merging the branch to master so code + docs unify before
starting V1.3.
